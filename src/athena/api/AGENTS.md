# API

## Purpose

Translate HTTP requests and events to the interface-neutral service contract.

## Contract

API code decodes, validates, streams, and serializes. It must not own task,
model, capability, or persistence decisions.

## Verification

Run API, SSE, admission, and authorization tests.
