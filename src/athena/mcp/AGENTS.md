# MCP

## Purpose

Adapt remote MCP servers and their resources/tools into Athena's contracts.

## Contract

MCP is an adapter, not an authority. Transport health, schemas, provenance,
and capability policy must remain observable and fail closed.

## Verification

Run MCP adapter, reconnect, and security tests.
